import React from "react";
import CardMolecul from "../../__molecules/Card/CardMolecul";

const CardOrganism = ({ show, setCount }) => {
  return <CardMolecul show={show} setCount={setCount} />;
};

export default CardOrganism;
